<% if @announcement.persisted? %>
	$('#main-box #tabbed-content').html("<%= escape_javascript(render 'places/announcements/listing').html_safe %>");	
	$('#main-box #status-bar .announcements').html("<%= escape_javascript(render 'places/announcements/count').html_safe %>");
<% end %>