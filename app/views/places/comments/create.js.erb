<% if user_signed_in? %>
	$('#main-box #tabbed-content').html("<%= escape_javascript(render 'places/comments/listing').html_safe %>");
	$('#main-box #status-bar .comments').html("<%= escape_javascript(render 'places/comments/count').html_safe %>");	
<% end %>
