<% if @comment.destroyed? %>
	$("#com-<%= @comment.id %>").fadeOut(200).delay(200).remove();
	ViewComponents.Notification.put("<p class='success'><%= t('comments.messages.deleted') %></p>");
<% end %>